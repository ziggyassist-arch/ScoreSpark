You are building ScoreSpark, a Next.js multi-sport live scores app.

TASK: Create a complete mock data layer so every page works end-to-end with static data.

FIRST: Read src/lib/types.ts to understand the existing types.

Then create these files:

1. src/lib/mock-data/teams.ts - 8-10 teams per sport (soccer/nba/nfl/nhl/mlb) with id, name, shortName, badge URLs (use https://placehold.co/64x64/1E1B3A/7EB6E6?text=ABBR), and full rosters (15-25 players each with id, name, position, jersey)

2. src/lib/mock-data/matches.ts - 10-15 matches per sport (mix upcoming/live/finished). Finished soccer matches need events (goals/cards/subs). American sports need linescores. Reference team IDs from teams.ts.

3. src/lib/mock-data/standings.ts - Full standings tables per league

4. src/lib/mock-data/players.ts - PlayerProfile for every player in every roster with stats

5. src/lib/mock-data/news.ts - 5+ news articles per sport

6. src/lib/mock-data/injuries.ts - 5+ injury reports per sport

7. src/lib/mock-data/leaders.ts - Stat leaders per sport

8. src/lib/mock-data/index.ts - Re-export everything

9. src/lib/services/mock-provider.ts - Functions matching existing service interfaces that return mock data

10. Wire into API routes: check USE_MOCK env var, return mock data if true

11. Set USE_MOCK=true in .env.local

Use REAL team/player names. Match existing TypeScript types EXACTLY. Every ID must be consistent across files so links work. Commit after each major file.

When done run: openclaw system event --text 'Done: Mock data layer complete' --mode now
